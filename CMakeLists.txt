PROJECT(conanlibgeotiff)
cmake_minimum_required(VERSION 2.8)
include(conanbuildinfo.cmake)
CONAN_BASIC_SETUP()
set(GEOTIFF_CSV_DATA_DIR ${CMAKE_SOURCE_DIR}/geotiff-1.4.2/csv)
add_subdirectory(geotiff-1.4.2)
install(FILES FindLibGeoTiff.cmake DESTINATION .)
